import {UserRound,ArrowRight} from "lucide-react"; import {useNavigate} from "react-router-dom";
export default function CandidateCard({candidate,index=0,onSelect,selectable=false,selected=false}){const nav=useNavigate();return <article className={`candidate-card ${selected?"selected":""}`} onClick={()=>selectable?onSelect(candidate):nav(`/candidates/${candidate.id}`)}>
 <div className="candidate-avatar"><UserRound size={34}/></div><div className="candidate-main"><div className="candidate-top"><span className="rank">0{index+1}</span><span className="symbol">{candidate.symbol}</span></div><h3>{candidate.fullName}</h3><p>{candidate.partyName}</p><div className="manifesto">{candidate.manifesto}</div><div className="candidate-foot">{selectable?<span>{selected?"✓ Selected":"Select candidate"}</span>:<span>View profile <ArrowRight size={15}/></span>}</div></div></article>}
