import {useEffect,useState} from "react"; import api from "../api"; import CandidateCard from "../components/CandidateCard"; import Loader from "../components/Loader";
export default function Candidates(){const [c,setC]=useState([]),[q,setQ]=useState("");useEffect(()=>{api.get("/candidates").then(x=>setC(x.data))},[]);const f=c.filter(x=>`${x.fullName} ${x.partyName}`.toLowerCase().includes(q.toLowerCase()));if(!c.length)return <Loader/>;return <main className="content-page"><div className="page-heading"><div><div className="eyebrow">CANDIDATE DIRECTORY</div><h1>Know your candidates.</h1><p>Review each candidate before making your choice.</p></div><input className="search" value={q} onChange={e=>setQ(e.target.value)} placeholder="Search candidate…"/></div><div className="candidate-grid">{f.map((x,i)=><CandidateCard key={x.id} candidate={x} index={i}/>)}</div></main>}
