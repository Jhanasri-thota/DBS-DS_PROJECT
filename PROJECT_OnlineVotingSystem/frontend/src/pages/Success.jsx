import {useLocation,useNavigate} from "react-router-dom"; import {CheckCircle2,Download,ArrowRight,ShieldCheck} from "lucide-react";
export default function Success(){const {state}=useLocation(),nav=useNavigate(),ref=state?.receiptCode||state?.transactionId||"VR-2026-DEMO";return <main className="success-page"><div className="success-icon"><CheckCircle2/></div><div className="eyebrow">TRANSACTION COMPLETE</div><h1>Vote cast successfully.</h1><p>Your vote has been securely recorded. Keep your transaction reference for your records.</p><div className="receipt-card"><div><small>Polling Reference</small><strong>{ref}</strong></div><div><small>Status</small><span className="badge success">✓ RECORDED</span></div><div><small>Time</small><b>{new Date(state?.votedAt||Date.now()).toLocaleString()}</b></div></div><div className="success-actions"><button className="primary-btn" onClick={()=>window.print()}><Download/> Print Receipt</button><button className="ghost-btn" onClick={()=>nav("/voter/dashboard")}>Return to Dashboard <ArrowRight/></button></div><div className="secure-note"><ShieldCheck/> Vote transaction recorded in the secure voting database.</div></main>}
