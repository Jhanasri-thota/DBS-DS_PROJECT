import {useEffect,useState} from "react"; import {useNavigate} from "react-router-dom"; import {ArrowRight,CheckCircle2,Clock,Vote,Users,ShieldCheck,Activity} from "lucide-react"; import api from "../api"; import Loader from "../components/Loader"; import StatusBadge from "../components/StatusBadge"; import CountdownTimer from "../components/CountdownTimer";
export default function Dashboard(){const nav=useNavigate(),[e,setE]=useState(null),[c,setC]=useState([]),[r,setR]=useState([]);const voter=JSON.parse(localStorage.getItem("voter")||'{"fullName":"Voter"}');useEffect(()=>{const load=()=>Promise.all([api.get("/election"),api.get("/candidates"),api.get("/results")]).then(([a,b,d])=>{setE(a.data);setC(b.data);setR(d.data)}).catch(()=>{});load();const i=setInterval(load,5000);return()=>clearInterval(i)},[]);if(!e)return <Loader/>;return <main className="dashboard"><div className="dash-head"><div><div className="eyebrow">VOTER DASHBOARD</div><h1>Good evening, {voter.fullName?.split(" ")[0]||"Voter"} 👋</h1><p>Your voter account is verified and ready.</p></div><StatusBadge>● VERIFIED VOTER</StatusBadge></div><div className="metric-grid"><div className="metric"><ShieldCheck/><span>Account</span><b>Verified</b><small>Identity & OTP complete</small></div><div className="metric"><Vote/><span>Vote status</span><b>{voter.hasVoted ? "Voted" : "Not yet voted"}</b><small>{voter.hasVoted ? "Vote already recorded" : "One vote permitted"}</small></div><div className="metric"><Users/><span>Candidates</span><b>{c.length}</b><small>Eligible candidates</small></div><div className="metric"><Activity/><span>Election</span><b>{e.status}</b><small>Live system status</small></div></div><div className="dashboard-grid"><section className="panel election-live"><div className="panel-label">CURRENT ELECTION <span className="live-dot small"><i/> LIVE</span></div><h2>{e.electionName}</h2><p>{e.description}</p><div className="date-row"><div><small>Voting started</small><b>{new Date(e.startTime).toLocaleString()}</b></div><div><small>Voting closes</small><b>{new Date(e.endTime).toLocaleString()}</b></div></div><CountdownTimer target={e.endTime}/><button className="primary-btn" onClick={()=>nav("/vote")}>Cast your vote <ArrowRight/></button></section><section className="panel"><div className="panel-label">LIVE STANDINGS</div>{r.slice(0,5).map((x,i)=><div className="standing" key={i}><span className="rank-circle">{i+1}</span><div><b>{x.candidateName||x.fullName}</b><small>{x.partyName||"Candidate"}</small></div><strong>{x.voteCount??x.votes??0}</strong></div>)}</section></div></main>}
