import {useState} from "react"; import {ChevronDown,HelpCircle} from "lucide-react";
const qs=[["How do I vote?","Login with your voter ID, verify the OTP, open the voting page, select one candidate and submit. A transaction reference is generated."],["What is the OTP?","For the academic demo, the OTP is 123456. Real production elections would require a secure external verification service."],["Can I change my vote?","No. The backend enforces one vote per voter. Review your selection carefully before submission."],["Is this connected to Aadhaar?","No. The identity-verification experience is simulated for academic demonstration only."]];
export default function Help(){const [o,setO]=useState(0);return <main className="content-page"><div className="page-heading centered-heading"><div className="auth-icon"><HelpCircle/></div><div className="eyebrow">HELP CENTER</div><h1>Questions, answered.</h1><p>Everything you need to know about the VoteSecure demo.</p></div><div className="faq">{qs.map(([q,a],i)=><div className="faq-item" key={q}><button onClick={()=>setO(o===i?-1:i)}><b>{q}</b><ChevronDown className={o===i?"rot":""}/></button>{o===i&&<p>{a}</p>}</div>)}</div></main>}
